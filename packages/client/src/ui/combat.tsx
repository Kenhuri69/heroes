/**
 * UI DOM du combat (doc 08 §2.4) — PLACEHOLDER de cadrage : le lot C livre
 * l'implémentation (bandeau armées + round, barre d'actions, prévisualisation
 * de dégâts, vitesses ×1/×2/×4, bouton Auto).
 */
export function CombatUi() {
  return null;
}
